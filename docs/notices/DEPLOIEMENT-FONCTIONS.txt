BAOBABS BASKET CLUB — Les fonctions serveur
===========================================

LE PIÈGE, ET IL EXPLIQUE TOUT
  Une fonction Supabase a DEUX noms.

    · « Name »  — l'étiquette affichée dans la liste. On la choisit, on
                  peut la changer, elle ne sert qu'à s'y retrouver.
    · le SLUG   — ce qui forme l'URL. Créée depuis l'éditeur du tableau
                  de bord, la fonction reçoit un slug TIRÉ AU SORT :
                  bright-endpoint, hyper-processor, swift-api…

  On déploie donc consciencieusement une fonction nommée
  « envoi-newsletter », le tableau de bord l'affiche sous ce nom — et
  elle répond en réalité à /functions/v1/bright-endpoint.

  L'administration, qui appelle un nom précis, recevait un 404 : le même
  résultat que si rien n'avait été déployé. La liste disait « c'est là »,
  la mesure disait « ça n'existe pas », et les deux avaient raison.

  Le slug se lit dans la colonne URL de la liste des fonctions, ou dans
  le fil d'Ariane quand on ouvre une fonction.


ÉTAT MESURÉ DU PROJET lmwbwasupqkvswukieav

  COMMENT ON L'A ÉTABLI — la méthode se refait en une minute. On envoie
  à chaque slug un POST au corps vide, avec la clé publique du site :
  chaque fonction répond par SON message d'erreur, qui la trahit. Aucune
  ne fait quoi que ce soit — le contrôle des arguments passe avant tout.

  Slug réel                Répond                            C'est donc
  ---------------------------------------------------------------------
  bright-endpoint          « sujet manquant »                newsletter
  hyper-processor          « identifiant d'inscription… »    alerte inscr.
  swift-api                « identifiant d'inscription… »    alerte, DOUBLON
  generate-text            INVALID_CREDENTIALS               aide rédaction
  send-order-confirmation  invalid_webhook_secret            commandes
  confirmation-reservation « reservation_id manquant »        DÉPLOYÉE
  depot-piece              « reference_ou_telephone_manquant » DÉPLOYÉE

  Remesuré le 3 septembre 2026, APRÈS déploiement en ligne de commande :
  les deux qui manquaient répondent. Et depot-piece répond 400, pas 401 :
  la requête a atteint le code de la fonction, donc verify_jwt = false a
  bien été pris en compte — c'est ce qui permet à une famille non
  connectée de déposer une pièce.

  Le côté SQL suit : bbc_piece_deposer existe. Comment on l'établit sans
  rien écrire — un POST au corps vide sur /rest/v1/rpc/<nom>, avec DEUX
  témoins. Un nom inventé reçoit un indice « Perhaps you meant… » ; un
  nom qui existe avec une autre signature n'en reçoit aucun.
  bbc_piece_deposer se comporte comme bbc_inscription, dont on sait
  qu'elle existe. Sans les deux témoins, la réponse ne prouve rien : les
  trois renvoient le même 404 PGRST202.

  L'admin appelle désormais les slugs réels : voir la table FN en tête
  du script de admin-matchs.html.


CE QUI RESTE À FAIRE
  1. FAIT le 3 septembre : confirmation-reservation et depot-piece sont
     déployées. Reste à vérifier qu'elles ENVOIENT — une fonction
     déployée sans les secrets Brevo répond mais n'envoie rien. Le seul
     essai qui tranche : une vraie réservation, et un vrai dépôt de
     pièce depuis « Suivre mon inscription ».

  2. SWIFT-API ET HYPER-PROCESSOR SONT LA MÊME FONCTION, déployée deux
     fois (à quinze jours et trois jours). La plus ancienne peut être
     supprimée — mais vérifiez d'abord qu'aucun déclencheur de la base
     ne l'appelle encore, sinon les alertes d'inscription cesseraient
     sans un mot.

  3. LES SECRETS BREVO, sans lesquels une fonction déployée répond mais
     n'envoie rien : BREVO_API_KEY, BBC_SENDER_EMAIL, et si l'on veut
     BBC_SENDER_NAME. Supabase → Edge Functions → Secrets.
     Ne les mettez jamais dans un fichier du dépôt.

  4. RIEN NE DÉCLENCHE L'ALERTE D'INSCRIPTION. La fonction est déployée
     et vivante (hyper-processor), mais personne ne l'appelle : ni la
     base, ni le site, ni l'admin. Une inscription attend donc qu'on
     pense à ouvrir l'admin — alors que la page promet un rappel sous
     48 h. Voir la section suivante.

  5. LE MÉNAGE DANS L'AUTRE PROJET. Le 2 septembre, un « deploy » sans
     --project-ref a envoyé les cinq fonctions dans mdg-creative-studio
     (ref fasdabfisaswvphgbhbn). Elles n'y font rien — les tables
     qu'elles cherchent n'y existent pas — mais elles encombrent :
         npx supabase functions delete <nom> --project-ref fasdabfisaswvphgbhbn
     pour aide-redaction, alerte-inscription, confirmation-reservation,
     depot-piece et envoi-newsletter.


QUE LES INSCRIPTIONS ARRIVENT DANS LA BOÎTE DU CLUB
  Tout est écrit et déployé. Il manque UNIQUEMENT le déclencheur : un
  webhook de base de données. Trois minutes, aucun code.

  1. LE SECRET DE DESTINATION
     Supabase → Edge Functions → Secrets → Add new secret
       Nom   : BBC_ALERT_EMAIL
       Valeur: l'adresse du club. Plusieurs adresses possibles,
               séparées par des virgules.
     Sans lui, l'alerte part sur BBC_SENDER_EMAIL — ce qui marche
     aussi, mais mélange l'expéditeur et le destinataire.

  2. LE WEBHOOK
     Supabase → Database → Webhooks → Create a new hook
       Name       : alerte-inscription
       Table      : academy_registrations
       Events     : Insert  (Update et Delete décochés)
       Type       : Supabase Edge Functions
       Edge Function : celle dont le slug est hyper-processor
       Method     : POST
     Laissez les en-têtes par défaut : Supabase y met sa clé de
     service, et c'est ce que la fonction attend.

  3. L'ESSAI
     Déposez une inscription depuis le site avec un nom d'essai. Elle
     doit arriver par e-mail en quelques secondes, avec la référence et
     le téléphone dans l'objet. Puis supprimez le dossier d'essai dans
     l'admin.
     Si rien n'arrive : Database → Webhooks → la colonne des envois
     récents dit le code de réponse. Un 500 « BREVO_API_KEY manquant »
     veut dire que le point 1 des secrets n'est pas fait.

  POURQUOI UN WEBHOOK ET PAS UN APPEL DEPUIS LE SITE
  La fonction relit la ligne dans la base à partir du seul identifiant :
  ce qui part par e-mail vient toujours de la base, jamais du corps de
  la requête. Appelée depuis le site, elle resterait sûre — mais
  n'importe qui pourrait faire renvoyer l'alerte en boucle. Le webhook
  part du serveur : personne d'autre ne peut le déclencher.


DÉPLOYER AVEC UN SLUG LISIBLE — LA LIGNE DE COMMANDE
  C'est la seule façon d'obtenir un slug qui soit le nom : déployée
  ainsi, la fonction prend le nom de son dossier.

  DEUX PIÈGES, PAYÉS LE 2 SEPTEMBRE 2026.

  · « deploy » SANS --project-ref propose une LISTE et déploie où vous
    cliquez. Le dossier était lié à un autre projet (mdg-creative-studio) ;
    les cinq fonctions y sont parties d'un coup. Sans dégât — elles y
    cherchent des tables qui n'existent pas — mais à supprimer :
        npx supabase functions delete <nom> --project-ref <le mauvais ref>
    Mettez TOUJOURS --project-ref, même quand le lien semble bon.

  · « 403 : your account does not have the necessary privileges » ne veut
    pas dire que la commande est fausse. Il veut dire que le compte
    connecté ne voit pas ce projet. Même « functions list », qui ne fait
    que lire, échoue. Pour trancher en une commande :
        npx supabase projects list
    Baobabs absent de la liste = mauvais compte. Présent mais deploy
    refusé = rôle en lecture seule sur l'organisation.
    Le projet Baobabs appartient au compte du club, pas au compte studio.

  Un terminal ouvert DANS le dossier du site :

      npx supabase login
      npx supabase functions deploy depot-piece --project-ref lmwbwasupqkvswukieav

  On nomme la fonction : « deploy » tout court enverrait AUSSI
  envoi-newsletter et alerte-inscription, qui tournent déjà sous
  bright-endpoint et hyper-processor. On se retrouverait avec des
  doublons, et il faudrait mettre à jour la table FN de l'admin dans la
  foulée. On répare ce qui est cassé, on ne touche pas à ce qui marche.

  La dernière commande envoie les quatre fonctions rangées dans
  supabase/functions/ (envoi-newsletter, confirmation-reservation,
  alerte-inscription, aide-redaction), chacune sous son propre nom.

  Le mot de passe demandé au « link » est celui de la BASE DE DONNÉES
  (Project Settings → Database), pas celui de votre compte.

  Une fois cela fait, remettez les vrais noms dans la table FN de
  admin-matchs.html — le commentaire qui l'accompagne le rappelle.


VÉRIFIER SANS RIEN ENVOYER
  L'écran Newsletter interroge la fonction à chaque ouverture. Tant
  qu'elle ne répond pas, un bandeau orange s'affiche ; il disparaît tout
  seul dès qu'elle répond. Cela ne coûte pas un e-mail.

  En ligne de commande :
      npx supabase functions list


ENSUITE
  Admin → Newsletter :
    1. Écrivez un sujet et un message.
    2. Mettez VOTRE adresse dans « M'envoyer un test » : seul vous
       recevez, avec [TEST] devant le sujet.
    3. Quand c'est bon : « Envoyer à tous ».

  Brevo en offre gratuite = 300 e-mails par jour. Au-delà de 300
  inscrits, l'envoi doit être étalé ou le compte passé en offre payante.
